{
  "name": "parallel-reader",
  "version": "1.0.0",
  "description": "Параллельный ридер с переводом и озвучкой",
  "main": "index.js",
  "scripts": {
    "start": "echo \"Откройте в Android Studio через: npx cap open android\"",
    "sync": "npx cap sync",
    "build:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/core": "^5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
}
